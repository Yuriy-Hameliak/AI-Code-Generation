def count_monkeys(n):
    """
    Populates an array with all numbers up to and including a certain number (n), 
    but excluding zero.

    Args:
        n: The number of monkeys in the forest.

    Returns:
        A list containing all numbers from 1 to n.
    """
    return list(range(1, n + 1))